// @ts-nocheck

import { redClient } from './red-client';
import { greenClient } from './green-client';
import { blueClient } from './blue-client';
import { blackClient } from './black-client';
import { common } from './common';

export default {
    redClient,
    greenClient,
    blueClient,
    blackClient,
    common
};
